'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // put all the migrations in one file
  },

  down: async (queryInterface, Sequelize) => {
    //await queryInterface.dropTable('Useras');
  }
};
